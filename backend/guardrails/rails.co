# We are relying exclusively on the 'self check input' flow defined in config.yml.
# The default intent-based off-topic checking (defining 'user ask off topic')
# has been removed because it was too aggressive and falsely flagged vague
# data questions (e.g. 'Give male vs female share') as off-topic.
